import { buildSiteMetadata } from "@/lib/sites"

export const metadata = buildSiteMetadata("twbst528")

export default function Twbst528Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
