import { buildSiteMetadata } from "@/lib/sites"

export const metadata = buildSiteMetadata("twjsz666")

export default function Twjsz666Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
