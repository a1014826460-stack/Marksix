# Legacy vs React Comparison

This document is kept only as historical reference.

The current production-facing foreground path is the legacy HTML + JS + CSS site under `public/vendor/shengshi8800/**`.
React homepage rendering is no longer the maintained main path and related unused files have been archived under `_archived_unused_frontend/`.
