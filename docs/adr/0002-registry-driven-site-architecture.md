# Registry-Driven Site Architecture

Frontend sites will be modeled through a site registry that defines each site's identity, route, assets, default lottery settings, render mode, and optional capabilities. Page entries and unified site API routes will resolve behavior through the registry and site adapters, while existing large UI components and vendor assets remain in place to reduce migration risk.
