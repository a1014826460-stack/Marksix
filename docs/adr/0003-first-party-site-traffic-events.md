# First-Party Site Traffic Events

Public site traffic will be collected as first-party traffic events and stored for management reporting instead of relying on an external analytics provider. The first phase includes page and article views plus compatibility API hit events, so the team can compare traffic across sites and see which legacy routes are still used before future migrations.
