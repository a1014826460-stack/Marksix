"""app_http/ - HTTP transport layer (request context, routing, auth, response).

This package was renamed from http/ to avoid shadowing the Python stdlib http package.
All imports from app_http.* resolve correctly in all contexts (pytest, direct execution, etc.).
"""
